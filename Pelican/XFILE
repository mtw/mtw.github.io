pelican content -o output -s pelicanconf.py

drop shadow via ImageMagick:
convert in.png \( +clone -background black -shadow 80x20+0+15 \) +swap -background transparent -layers merge +repage out.webp
