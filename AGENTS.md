# Repository Instructions

- Stage and commit completed changes by default unless the user explicitly asks not to.
- Prefer repository-relevant tests over bundled upstream theme snapshot suites.
- Treat `Pelican/` as the source of truth for site content and build configuration.
